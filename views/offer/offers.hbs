<h1><span class="badge badge-secondary badge-pill badge-dark">Страница оформления заказа:</span></h1><br>

Вы выбрали следующие позиции:
    <div class="entry" id = "{{offer._id}}"></div>
      {{!-- <img src="{{}}" alt="logoRestoran"> --}}
      <ul class="contents">
      {{#each offer.contents}}
      <li>{{this}}</li>
      {{/each}}
      </ul>
    </li>
    <div>Стоимость Вашего заказа составила: {{offer.price}} рублей</div>
    <div>Дата и время оформления заказа: {{offer.createdAt}}</div>
    <a href="./offers/{{_id}}"></a>

    <div>Для оплаты заказа нажмите кнопку "Оплатить"</div>
    <button class="btn btn btn-dark">Оплатить заказ</button>
  
